# Native app (Expo) — copy to .env and fill in your Supabase project values.
# EXPO_PUBLIC_* vars are embedded in the built app (client-safe publishable/anon key — RLS enforced).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-publishable-anon-key
